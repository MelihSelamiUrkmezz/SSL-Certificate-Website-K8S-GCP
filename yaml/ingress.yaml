apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webappingress
spec:
  tls:
  - secretName: firstsecret
  hosts: dtcloudintern.com
  defaultBackend:
    service:
      name: "webappservice"
      port:
        number: 80
